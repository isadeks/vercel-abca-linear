version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci --no-audit --no-fund
    build:
      commands:
        - npm run lint
        - npm test
  artifacts:
    baseDirectory: .
    files:
      - "*.html"
  cache:
    paths:
      - node_modules/**/*
