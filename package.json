{
  "name": "wander-travel",
  "version": "1.0.0",
  "private": true,
  "description": "Wander travel guide site + booking API (AWS Amplify hosted).",
  "type": "module",
  "scripts": {
    "lint": "eslint . --max-warnings=0",
    "test": "vitest run"
  },
  "devDependencies": {
    "eslint": "^9.13.0",
    "vitest": "^2.1.4"
  }
}
